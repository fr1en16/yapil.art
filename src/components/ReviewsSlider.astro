---
import ReviewsSliderComponent from './ui/ReviewsSlider';

interface Props {
  theme?: 'dark' | 'light';
  lang?: 'ru' | 'en';
}

const { theme = 'dark', lang = 'ru' } = Astro.props;
---

<ReviewsSliderComponent client:visible theme={theme} lang={lang} />
